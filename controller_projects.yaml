---
controller_projects:
  - name: "{{ orgs }} Demo Project"
    description: "Demo Project"
    organization: "{{ orgs }}"
    scm_type: git
    scm_url: "git@github.com:automationiberia/aap-workflow-multicloud.git"
    scm_credential: "{{ orgs }} Gitlab Credential"
    scm_branch: "main"
    scm_credential: "{{ orgs }} Gitlab Credential"
    scm_clean: false
    scm_delete_on_update: false
    scm_update_on_launch: false
    scm_update_cache_timeout: 86400
    allow_override: true
...
