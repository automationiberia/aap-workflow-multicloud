---
controller_inventories:
  - name: "{{ orgs }} Demo Inventory OCP"
    description: "Demo Inventory OCP"
    organization: "{{ orgs }}"

  - name: "{{ orgs }} Demo Inventory EKS"
    description: "Demo Inventory EKS"
    organization: "{{ orgs }}"
...
