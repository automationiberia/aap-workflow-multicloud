---
controller_hosts:
  - name: localhost
    description: localhost
    inventory: "{{ orgs }} Demo Inventory OCP"

  - name: localhost
    description: localhost
    inventory: "{{ orgs }} Demo Inventory EKS"
...
